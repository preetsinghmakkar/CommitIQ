# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @commit-iq/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t commit-iq-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system commit-iq-auth-service && \
          adduser --system -G commit-iq-auth-service commit-iq-auth-service

COPY apps/auth-service/dist commit-iq-auth-service/
COPY apps/auth-service/package.json commit-iq-auth-service/
RUN chown -R commit-iq-auth-service:commit-iq-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix commit-iq-auth-service --omit=dev -f install

CMD [ "node", "commit-iq-auth-service" ]
